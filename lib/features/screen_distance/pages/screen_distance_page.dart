import 'package:flutter/material.dart';

class ScreenDistancePage extends StatelessWidget {
  const ScreenDistancePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Screen Distance'),
      ),
      body: const Center(
        child: Text('Screen Distance Page'),
      ),
    );
  }
}
